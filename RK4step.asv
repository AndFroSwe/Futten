function [tout, uout] = RK4step (t, u, h)
% RK4STEP (t, u, h) calculates a step with RK4 method with step length h.  

% RK4 factors
k1 = F(t, u);
k2 = F(t+0.5*h, u+h*0.5*k1);
k3 = F(t+0.5*h, u+h*0.5*k2);
k4 = F(t+h, u+k3*h);

uout = u + h*(k1+2*k2+2*k3+k4)/6;
tout = t + h;

